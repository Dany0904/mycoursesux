define("local_mycoursesux/main",[],(function(){return{init:function(){const items=document.querySelectorAll(".mycoursesux-item"),searchInput=document.getElementById("mycoursesux-search-input"),sortSelect=document.getElementById("mycoursesux-sort-select"),filterButtons=document.querySelectorAll("[data-filter]"),layoutButtons=document.querySelectorAll("[data-layout]"),container=document.getElementById("mycoursesux-container");let currentFilter="all";function applyFilters(){const search=searchInput.value.toLowerCase();items.forEach((item=>{const name=item.dataset.name.toLowerCase(),status=item.dataset.status;let visible=!0;"all"===currentFilter||status.includes(currentFilter)||(visible=!1),name.includes(search)||(visible=!1),item.style.display=visible?"":"none"}))}filterButtons.forEach((btn=>{btn.addEventListener("click",(()=>{filterButtons.forEach((b=>b.classList.remove("active"))),btn.classList.add("active"),currentFilter=btn.dataset.filter,applyFilters()}))})),searchInput&&searchInput.addEventListener("input",applyFilters),sortSelect&&sortSelect.addEventListener("change",(function(){const value=sortSelect.value,sorted=Array.from(items);sorted.sort(((a,b)=>"name"===value?a.dataset.name.localeCompare(b.dataset.name):"progress"===value?b.dataset.progress-a.dataset.progress:0)),sorted.forEach((el=>container.appendChild(el)))})),layoutButtons.forEach((btn=>{btn.addEventListener("click",(()=>{const layout=btn.dataset.layout;window.location.search="?layout="+layout}))}))}}}));

//# sourceMappingURL=main.min.js.map